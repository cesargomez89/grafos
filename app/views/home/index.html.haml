%h3
  Lists
  = link_to new_list_path, class: 'btn btn-primary btn-sm' do
    %i.glyphicon.glyphicon-plus
    New Graph
.list-group
  - @lists.each do |list|
    = link_to list.name, list_path(list), class: 'list-group-item list-group-item-info'
- if @lists.empty?
  = render partial: 'layouts/empty_list'
